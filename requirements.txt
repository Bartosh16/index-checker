# System requirements for Index Checker
Node.js >= 22.13 recommended
npm >= 11
PostgreSQL >= 16 or Docker Desktop with Docker Compose (optional)
Google Search Console property access
Google service account with webmasters.readonly access (if using GSC)
SERP provider API key such as Serper / SerpApi / DataForSEO (if using SERP checks)
